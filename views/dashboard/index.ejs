<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TripWheels Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        /* Only essential CSS for gradient and shadow effects */
        .navbar-custom {
            background: linear-gradient(135deg, #4361ee, #3f37c9);
        }

        .sidebar-header {
            background: linear-gradient(135deg, #4361ee, #3f37c9);
        }

        .welcome-title {
            background: linear-gradient(135deg, #4361ee, #3f37c9);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }
    </style>
</head>

<body class="bg-light">
    <!-- Include Header -->
    <%- include('header'); %>

    <!-- Dashboard Content -->
    <div class="container-fluid py-4">
        <div class="row">
            <!-- Include Sidebar -->
            <%- include('sidebar'); %>

            <!-- Main Content -->
            <div class="col-lg-9 col-md-8">
                <div class="d-flex align-items-center justify-content-center min-vh-75">
                    <div class="text-center">
                        <i class="fas fa-tachometer-alt text-primary opacity-75 mb-4" style="font-size: 8rem;"></i>
                        <h1 class="welcome-title display-4 fw-bold mb-3">Welcome to Dashboard</h1>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>