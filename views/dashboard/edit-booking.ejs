<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Booking - TripWheels Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        .navbar-custom {
            background: linear-gradient(135deg, #4361ee, #3f37c9);
        }
        .sidebar-header {
            background: linear-gradient(135deg, #4361ee, #3f37c9);
        }
        .edit-form {
            max-width: 900px;
            margin: 0 auto;
        }
        .booking-header {
            background: linear-gradient(135deg, #4361ee, #3f37c9);
            color: white;
            padding: 20px;
            border-radius: 10px 10px 0 0;
        }
    </style>
</head>
<body class="bg-light">
    <%- include('header'); %>

    <div class="container-fluid py-4">
        <div class="row">
            <%- include('sidebar'); %>

            <div class="col-lg-9 col-md-8">
                <div class="card shadow-sm border-0">
                    <div class="booking-header">
                        <div class="d-flex justify-content-between align-items-center">
                            <div>
                                <h3 class="mb-0"><i class="fas fa-edit me-2"></i>Edit Booking</h3>
                                <small>Booking ID: <%= booking._id %></small>
                            </div>
                            <a href="/my-bookings" class="btn btn-light btn-sm">
                                <i class="fas fa-arrow-left me-1"></i>Back
                            </a>
                        </div>
                    </div>
                    
                    <div class="card-body">
                        <form method="POST" action="/update-booking/<%= booking._id %>">
                            <div class="row">
                                <div class="col-md-6 mb-3">
                                    <label class="form-label">Customer Name</label>
                                    <input type="text" name="userName" class="form-control" value="<%= booking.userName %>" required>
                                </div>
                                <div class="col-md-6 mb-3">
                                    <label class="form-label">Customer Email</label>
                                    <input type="email" name="userEmail" class="form-control" value="<%= booking.userEmail %>" required>
                                </div>
                                <div class="col-md-6 mb-3">
                                    <label class="form-label">Customer Phone</label>
                                    <input type="text" name="userPhone" class="form-control" value="<%= booking.userPhone %>" required>
                                </div>
                                <div class="col-md-6 mb-3">
                                    <label class="form-label">Vehicle Type</label>
                                    <select class="form-select" name="vehicleType" required>
                                        <option value="Hatchback" <%= booking.vehicleType === 'Hatchback' ? 'selected' : '' %>>Hatchback</option>
                                        <option value="Sedan" <%= booking.vehicleType === 'Sedan' ? 'selected' : '' %>>Sedan</option>
                                        <option value="SUV" <%= booking.vehicleType === 'SUV' ? 'selected' : '' %>>SUV</option>
                                        <option value="Innova" <%= booking.vehicleType === 'Innova' ? 'selected' : '' %>>Innova</option>
                                        <option value="Tempo Traveller" <%= booking.vehicleType === 'Tempo Traveller' ? 'selected' : '' %>>Tempo Traveller</option>
                                        <option value="Bus" <%= booking.vehicleType === 'Bus' ? 'selected' : '' %>>Bus</option>
                                    </select>
                                </div>
                                <div class="col-md-6 mb-3">
                                    <label class="form-label">Pickup City</label>
                                    <input type="text" name="pickupCity" class="form-control" value="<%= booking.pickupCity %>" required>
                                </div>
                                <div class="col-md-6 mb-3">
                                    <label class="form-label">Drop City</label>
                                    <input type="text" name="dropCity" class="form-control" value="<%= booking.dropCity %>" required>
                                </div>
                                <div class="col-md-4 mb-3">
                                    <label class="form-label">Total Amount (₹)</label>
                                    <input type="number" name="totalAmount" class="form-control" value="<%= booking.totalAmount %>" required>
                                </div>
                                <div class="col-md-4 mb-3">
                                    <label class="form-label">Advance Paid (₹)</label>
                                    <input type="number" name="advancePaid" class="form-control" value="<%= booking.advancePaid %>" required>
                                </div>
                                <div class="col-md-4 mb-3">
                                    <label class="form-label">Status</label>
                                    <select class="form-select" name="status" required>
                                        <option value="Pending" <%= booking.status === 'Pending' ? 'selected' : '' %>>Pending</option>
                                        <option value="Confirmed" <%= booking.status === 'Confirmed' ? 'selected' : '' %>>Confirmed</option>
                                        <option value="Cancelled" <%= booking.status === 'Cancelled' ? 'selected' : '' %>>Cancelled</option>
                                        <option value="Completed" <%= booking.status === 'Completed' ? 'selected' : '' %>>Completed</option>
                                        <option value="In Progress" <%= booking.status === 'In Progress' ? 'selected' : '' %>>In Progress</option>
                                    </select>
                                </div>
                                <div class="col-md-6 mb-3">
                                    <label class="form-label">Payment Status</label>
                                    <select class="form-select" name="paymentStatus" required>
                                        <option value="Pending" <%= booking.paymentStatus === 'Pending' ? 'selected' : '' %>>Pending</option>
                                        <option value="Partially Paid" <%= booking.paymentStatus === 'Partially Paid' ? 'selected' : '' %>>Partially Paid</option>
                                        <option value="Paid" <%= booking.paymentStatus === 'Paid' ? 'selected' : '' %>>Paid</option>
                                        <option value="Refunded" <%= booking.paymentStatus === 'Refunded' ? 'selected' : '' %>>Refunded</option>
                                    </select>
                                </div>
                                <div class="col-md-6 mb-3">
                                    <label class="form-label">Admin Notes</label>
                                    <textarea class="form-control" name="adminNotes" rows="3"><%= booking.adminNotes || '' %></textarea>
                                </div>
                            </div>
                            
                            <div class="d-flex justify-content-between mt-4">
                                <a href="/my-bookings" class="btn btn-secondary">Cancel</a>
                                <button type="submit" class="btn btn-primary">Update Booking</button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>